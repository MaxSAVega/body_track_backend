spring.application.name=body_track_backend

!Backend de Railway
#spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}
#spring.datasource.username=${MYSQLUSER}
#spring.datasource.password=${MYSQLPASSWORD}

!Backend de mi laptop
spring.datasource.url=jdbc:mysql://localhost:3306/body_track
spring.datasource.username=root
spring.datasource.password=12345

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

server.port=${PORT:8080}

! === File Upload Configuration ===
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=${UPLOAD_DIR:./uploads/anomalies}

! === JWT Configuration ===
! Para PRODUCCIÓN: setear JWT_SECRET como variable de entorno (Railway: Railway CLI o Dashboard)
! Generar secreto con: openssl rand -base64 32
! ⚠️  No usar este valor por defecto en producción
jwt.secret=${JWT_SECRET:dGhpcyBpcyBhIGRldmVsb3BtZW50IG9ubHkgc2VjcmV0IGRvIG5vdCB1c2UgaW4gcHJvZCA6KQ==}
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=604800000